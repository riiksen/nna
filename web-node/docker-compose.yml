version: '3'

services:
  app:
    build: ./
    image: mean
    expose:
      - 4040
    environment:
      NODE_ENV: production
      SERVER_PORT: 4040
    restart: always
    depends_on:
      - mongo

  mongo:
    image: mongo:3.6
